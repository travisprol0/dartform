# DartForm

Web app (Vite + React). Uses browser camera and MediaPipe Pose Landmarker WASM.

Before changing camera or pose code, check [@mediapipe/tasks-vision](https://www.npmjs.com/package/@mediapipe/tasks-vision) docs.
